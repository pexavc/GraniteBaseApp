import Granite

extension ConfigService.Center {
    struct Boot: GraniteReducer {
        typealias Center = ConfigService.Center
        
        func reduce(state: inout Center.State) {
            
        }
    }
}
